<div class="review-rating" data-score="<%= review.rating %>"></div>
<p><%= review.comment%></p>

<% if user_signed_in? %>

<% if review.user_id == current_user.id %>

<%= link_to "Edit", edit_workout_review_path(review.workout, review) %>
<%= link_to "Delete", workout_review_path(review.workout, review), method: :delete, data: {conform: "Are you sure?" } %>

<% end %>

<% end %>